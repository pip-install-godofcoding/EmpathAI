# Copy this file to .env and fill in your key(s). Do NOT commit your real .env to source control.
# Example settings to enable Gemini provider proxying
# GEMINI_API_URL should be the full HTTP endpoint you want the server to call.
# GEMINI_API_KEY can be provided in x-api-key header; alternatively provide GEMINI_BEARER_TOKEN for Authorization: Bearer

GEMINI_API_URL=https://your-gemini-endpoint.example/v1/generate
GEMINI_API_KEY=
# or
# GEMINI_BEARER_TOKEN=

# Optional: provider selection
LLM_PROVIDER=gemini

# Optional: a JSON string template for payload the provider expects. Use ${message} to include user message.
# Example: {"prompt":"${message}","temperature":0.7}
GEMINI_API_PAYLOAD_TEMPLATE=
